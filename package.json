{
  "name": "sreewoodworks-root",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  }
}
